# Universal Unit Converter

Convert between units of measurement (length, mass, time, etc.) from the command line.

## Features
- Simple CLI interface
- Supports multiple unit types (expandable)
- Clean error handling

## Usage
```bash
python main.py 10 --from-unit m --to-unit km
```

## Contributing
Expand the conversion factors in `main.py` to add more units.